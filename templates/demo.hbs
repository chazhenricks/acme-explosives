 <thead>
    <tr>
        <th colspan="9"> {{categories.[1].name}}</th>
    </tr>
</thead>
<tbody>
    <tr>
        {{#each types}}
            {{#if (eq category 1)}}
            <td colspan='3'>{{name}}</td>
            {{/if}}
        {{/each}}
    </tr>
    <tr>
        <td colspan='3'>{{types.[3].description}}</td>
        <td colspan='3'>{{types.[4].description}}</td>
        <td colspan='3'>{{types.[5].description}}<td>
    </tr>
    <tr>
        {{#each products}}
            {{#if (eq type 3)}}
            <td> {{name}} </td>
            {{/if}}
        {{/each}}
         {{#each products}}
            {{#if (eq type 4)}}
            <td> {{name}} </td>
            {{/if}}
        {{/each}}
         {{#each products}}
            {{#if (eq type 5)}}
            <td> {{name}} </td>
            {{/if}}
        {{/each}}
    </tr>
    <tr>
        {{#each products}}
            {{#if (eq type 3)}}
            <td> {{description}} </td>
            {{/if}}
        {{/each}}
         {{#each products}}
            {{#if (eq type 4)}}
            <td> {{description}} </td>
            {{/if}}
        {{/each}}
         {{#each products}}
            {{#if (eq type 5)}}
            <td> {{description}} </td>
            {{/if}}
        {{/each}}
    </tr>
</tbody>
